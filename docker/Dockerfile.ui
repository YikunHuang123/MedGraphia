# ============================================================================
# MedGraphia — Streamlit UI image
# Lightweight: only needs requests, streamlit, and graph visualisation libs
# ============================================================================

FROM python:3.12-slim AS runtime

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends curl \
    && rm -rf /var/lib/apt/lists/*

# Install only UI dependencies (subset of pyproject.toml)
RUN pip install --no-cache-dir \
    streamlit>=1.35.0 \
    httpx>=0.27.0 \
    pydantic>=2.7.0 \
    pyvis>=0.3.2 \
    pandas>=2.2.0 \
    plotly>=5.22.0

COPY src/medgraphia/ui/ /app/ui/

ENV PYTHONPATH=/app
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

EXPOSE 8501

CMD ["streamlit", "run", "/app/ui/streamlit_app.py", \
     "--server.port=8501", "--server.address=0.0.0.0", \
     "--server.headless=true"]
